node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
.env*
!.env.example
next-env.d.ts
tsconfig.tsbuildinfo
_research
_qa

# Netlify CLI working directories (`netlify dev`, `netlify build`).
.netlify

# Local account storage for the `fs` driver, and the development email outbox. Holds
# password hashes, session records and verification tokens, so it must never be
# committed. Relocate it with CINEORA_DATA_DIR.
.data
